import React from "react";

export const Schedule = () => {
  const styles = getStyles();
  return (
    <div style={styles.container}>
      <h1>Schedule</h1>
    </div>
  );
};

const getStyles = () => ({
  container: {},
});
