import React from "react";

export const PlayersList = () => {
  const styles = getStyles();
  return (
    <div style={styles.container}>
      <h1>PlayersList</h1>
    </div>
  );
};

const getStyles = () => ({
  container: {},
});
